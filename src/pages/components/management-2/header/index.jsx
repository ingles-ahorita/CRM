export default function Header() {
  return (
    <div className="max-w-3xl">
      <h1 className="text-[28px] font-extrabold tracking-tight text-[#0f172a] sm:text-[32px] -mt-10">
        Management Dashboard
      </h1>
      {/* <p className="mt-2.5 max-w-5xl text-[14px] font-medium leading-relaxed text-slate-500">
        Includes ALL elements from current Quick view + new per-closer snapshot
        grid. Visuals (sparklines, donuts, gauges, funnels, line charts) used
        wherever possible instead of raw numbers.
      </p> */}
    </div>
  );
}
